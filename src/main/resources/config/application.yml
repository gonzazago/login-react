spring:
  datasource:
    url: jdbc:mysql://localhost:3306/loginapp?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
    username: root
    password: root

  jpa:
    database: MYSQL
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect


  server: 8080
  jackson:
    time-zone: UTC
